# 1.1 Decision Matrix

## Results

| Local exec works | Remote exec works | Decision |
|---|---|---|
| ✓ (HTTP 403 — egress confirmed, bot perms issue) | ⏳ BLOCKED (ntn CLI auth required) | **PENDING — block for ntn login** |

## Evidence

### Local: ✓ Egress confirmed
- `fetch()` to `https://discord.com/api/v10/channels/1504266202506199272` returned HTTP 403
- 403 "Missing Access" confirms the request reached Discord, was authenticated,
  but the bot lacks channel permissions
- This is NOT a network/sandbox restriction — it's a Discord RBAC issue

### Remote: ⏳ Requires human action
- `ntn` CLI cannot authenticate from the worker sandbox (keychain inaccessible)
- Human must run `ntn login` interactively, then:
  1. `ntn workers env set DISCORD_BOT_TOKEN=<token>`
  2. Deploy the spike capability
  3. `ntn workers exec spike/discordPing`
- This test determines whether Notion's cloud runtime blocks outbound HTTPS

## Preliminary Decision
Given local egress is confirmed, the architecture question is:
**Does the Notion Workers CLOUD runtime also allow egress?**

If yes → Row 1: proceed with 1.7 webhook + 1.3 direct-fetch sync
If no  → Row 2: Workers runtime is sandboxed; relay needed
