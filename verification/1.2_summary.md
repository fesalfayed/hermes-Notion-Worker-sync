# 1.2 Verification — orchestrator-direct completion 2026-05-15

## Deliverable status
- ✅ `src/index.ts` declares `worker.database("projects", ...)` with `primaryKeyProperty: "discord_channel_id"` and all 10 required schema properties.
- ✅ Schema helpers used (from `~/Desktop/Notion CLI Docs/schema.md`): `Schema.title`, `Schema.richText`, `Schema.select`, `Schema.checkbox`, `Schema.date`. All 5 are documented in the SoT.
- ✅ Deploy succeeded — workerId `019e2a23-71c0-70e4-b04e-0e15659ba93a`, updatedAt `2026-05-15T06:15:34.021Z` (post-database-merge).
- ✅ `workers.json` corrected to canonical workerId (the original Phase 1.0 deploy created a stale workerId 019e2a18-... that was orphaned during sandbox-worker conflict; canonical from `ntn workers list` is now in workers.json).

## CLI introspection caveat (spec defect — orchestrator-acknowledged)
The original card body specified `ntn workers databases list --json` to dump the data source ID — that subcommand does not exist in the current `ntn` CLI surface. The actual CLI surface (per `ntn workers --help`) is: capabilities, create, delete, deploy, new, env, exec, get, oauth, list, runs, sync, usage, webhooks, tui.

Database introspection is implicit: `worker.database()` declarations are not separately listable via CLI; their existence is proven by:
1. Successful deploy (database migrations run server-side; failures would abort deploy).
2. Notion-UI inspection (the "Hermes Projects" managed DB appears in the workspace, owned by the worker).
3. Successful sync runs writing to it (covered in 1.3).

`verification/1.2_capabilities.json` shows the worker has no tools/syncs yet (`[]`), which is expected — 1.2 only declares the DB schema. Syncs land in 1.3, tools land in 1.4–1.6.

## 10 properties (verified by reading src/index.ts)
1. `Name` — title
2. `discord_channel_id` — richText (primary key)
3. `discord_topic` — richText
4. `kanban_board_slug` — richText
5. `kanban_task_ids` — richText
6. `status` — select (6 options: Backlog, Planning, In progress, Paused, Done, Cancelled)
7. `discord_archived` — checkbox
8. `discord_category_id` — richText
9. `last_discord_edit` — date
10. `notes` — richText

## Completion path
Card completed orchestrator-direct due to protocol_violation on worker exit (worker hit the non-existent `databases list` command, exited cleanly without complete/block). The deliverable IS landed; the verification command was malformed in the spec, not the implementation.
